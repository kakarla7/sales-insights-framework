name: Audience A CI

on:
  push:
    branches:
      - audience-a-iat
      - audience-a-prod
    paths:
      - "audiences/audience_a/**"
      - "shared/insights_framework/**"
  pull_request:
    branches:
      - audience-a-dev
      - audience-a-iat
    paths:
      - "audiences/audience_a/**"
      - "shared/insights_framework/**"

jobs:
  test:
    name: Audience A — install, unit tests, integration tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install shared framework
        run: pip install -e shared/insights_framework

      - name: Install test dependencies
        run: pip install pytest pyyaml

      - name: Run shared unit tests
        run: pytest shared/insights_framework/tests/ -v

      - name: Run Audience A integration tests
        run: |
          cd audiences/audience_a
          pytest tests/ -v
